<?xml version="1.0" encoding="utf-8"?>
<!-- Tools tab — same shape as the vault folder screen: header, then a 2-column grid of
     gradient tiles (one per tool) with the native ad taking the second row. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:orientation="vertical">

    <!-- ===== Header ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="16dp"
        android:paddingEnd="20dp">

        <TextView
            style="@style/TextAppearance.App.Bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/lv_nav_tools"
            android:textColor="@color/home_title"
            android:textSize="28sp" />

        <TextView
            style="@style/TextAppearance.App.Medium"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="@string/tools_header_subtitle"
            android:textColor="@color/home_subtitle"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- ===== Tool tiles ===== -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerTools"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="8dp"
        android:paddingTop="12dp"
        android:paddingBottom="10dp"
        android:scrollbars="none"
        tools:listitem="@layout/item_vault_folder" />

</LinearLayout>
