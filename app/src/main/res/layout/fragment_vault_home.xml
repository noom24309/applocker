<?xml version="1.0" encoding="utf-8"?>
<!-- Vault tab — folder screen: header with live totals, search over folder names, and a
     2-column grid of folder tiles ending in "Create folder". -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:orientation="vertical">

    <!-- ===== Header ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingTop="16dp"
        android:paddingEnd="20dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                style="@style/TextAppearance.App.Bold"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/lv_vault_title"
                android:textColor="@color/home_title"
                android:textSize="28sp" />

            <TextView
                android:id="@+id/tvVaultSummary"
                style="@style/TextAppearance.App.Medium"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/home_subtitle"
                android:textSize="13sp"
                tools:text="142 items · 7 folders" />
        </LinearLayout>

        <FrameLayout
            android:id="@+id/btnAdd"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_perm_badge"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:contentDescription="@string/lv_vault_add_media"
                android:src="@drawable/ic_add"
                app:tint="@color/home_title" />
        </FrameLayout>
    </LinearLayout>

    <!-- ===== Search ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_folder_create"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="14dp">

        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_search"
            app:tint="@color/home_hint" />

        <EditText
            android:id="@+id/searchInput"
            style="@style/TextAppearance.App.Medium"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/lv_vault_search_hint"
            android:imeOptions="actionSearch"
            android:importantForAutofill="no"
            android:inputType="text"
            android:maxLines="1"
            android:textColor="@color/home_title"
            android:textColorHint="@color/home_hint"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- ===== Folder grid ===== -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerFolders"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingHorizontal="8dp"
            android:paddingTop="8dp"
            android:paddingBottom="10dp"
            android:scrollbars="none"
            tools:listitem="@layout/item_vault_folder" />

        <TextView
            android:id="@+id/tvFoldersEmpty"
            style="@style/TextAppearance.App.Medium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/lv_vault_no_folders"
            android:textColor="@color/home_subtitle"
            android:visibility="gone"
            tools:visibility="visible" />
    </FrameLayout>

</LinearLayout>
